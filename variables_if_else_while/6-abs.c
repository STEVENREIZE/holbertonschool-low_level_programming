#include "main.h"

/**
 * _abs - Computes the absolute value of an interger.
 * @n: The interger to be computed.
 *
 * Return: The absolue value of the interger.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
